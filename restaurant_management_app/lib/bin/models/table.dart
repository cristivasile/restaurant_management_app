class Table {
  late String id;
  late double xOffset, yOffset;
  late int tableSize;

  Table(
      {required this.id,
      required this.xOffset,
      required this.yOffset,
      required this.tableSize});
}